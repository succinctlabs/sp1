[package]
name = "patch-testing-tests"
version = "1.0.0" 
edition = "2021" 
publish = false

[dependencies]
sp1-sdk = { path = "../../crates/sdk" }

[build-dependencies]
sp1-build = { path = "../../crates/build" }
