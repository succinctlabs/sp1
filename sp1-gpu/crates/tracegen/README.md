# sp1-gpu-tracegen

Core GPU trace generation for SP1 machine traces.

Generates execution traces on GPU for SP1's virtual machine, converting program execution into the polynomial representations needed for proving.

---

Part of [SP1-GPU](https://github.com/succinctlabs/sp1/tree/dev/sp1-gpu), the GPU-accelerated prover for SP1.
