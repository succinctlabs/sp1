[workspace]
[package]
name = "panic-test"
version = "0.0.2-test"
edition = "2021"
publish = false

[dependencies]
sp1-zkvm = { path = "../../zkvm/entrypoint", version = "0.0.2-test" }
sp1-derive = { path = "../../derive", version = "0.0.2-test" }
