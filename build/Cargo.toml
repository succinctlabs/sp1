[package]
name = "sp1-build"
version = "0.1.0"
edition = "2021"

[dependencies]
cargo_metadata = "0.18.1"
anyhow = { version = "1.0.83" }
clap = { version = "4.5.7", features = ["derive", "env"], optional = true }


[features]
clap = ["dep:clap"]
