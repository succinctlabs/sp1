[package]
name = "panic-c-script"
description = "Host driver for panic-c. Runs both the success path (flag=0) and the abort() path (flag=1)."
version = { workspace = true }
edition = { workspace = true }
publish = false

[[bin]]
name = "panic-c-execute"
path = "src/execute.rs"

[[bin]]
name = "panic-c-prove"
path = "src/prove.rs"

[dependencies]
sp1-sdk = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[build-dependencies]
zkevm-c-build = { path = "../../c-build" }
