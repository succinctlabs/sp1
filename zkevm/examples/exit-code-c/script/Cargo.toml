[package]
name = "exit-code-c-script"
description = "Host driver for exit-code-c. Runs both the success path (flag=0) and the `return 1` path (flag=1)."
version = { workspace = true }
edition = { workspace = true }
publish = false

[[bin]]
name = "exit-code-c-execute"
path = "src/execute.rs"

[[bin]]
name = "exit-code-c-prove"
path = "src/prove.rs"

[dependencies]
sp1-sdk = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[build-dependencies]
zkevm-c-build = { path = "../../c-build" }
