[workspace]

[package]
name = "untrusted-program-jit-program"
version = "1.1.0"
edition = "2021"
publish = false

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
