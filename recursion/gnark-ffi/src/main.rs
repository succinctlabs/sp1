use sp1_recursion_gnark_ffi::ffi;

fn main() {
    unsafe {
        // ffi::Test2();
        // let res = ffi::Test3(2);
        // println!("res: {}", res);
        // let res2 = ffi::babybearextinv(2, 2, 2, 2, 2);
        // println!("res2: {}", res2);
        // // sp1_recursion_gnark_ffi::ffi::Groth16Proof {
        // //     PublicInputs: ["a".to_string(), "b".to_string()],
        // //     EncodedProof: "c".to_string(),
        // //     RawProof: "d".to_string(),
        // // };

        // ffi::prove_groth_16();
    }
}
